# Minimal tick entry point — visual-only, bypasses the broken rune/gate detection
# logic in moongate:tick until that's fixed. Gates are teleport-potion destinations
# only for now; this just keeps them looking alive.
function moongate:gate/ambient_glow
function moongate:gate/check_dwell
