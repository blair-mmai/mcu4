# virtues:fixtures/tick
# Independent dispatcher for persistent world fixtures (auto-refilling
# containers, etc.) — deliberately separate from puzzles/ and the NPC/
# conversation tick chain. Called once per tick from the main tick.mcfunction.

function virtues:fixtures/trial_key_chest_check
